import type { TextAreaProps, TextAreaStyle } from './props';
import { NewComponentTypes, SharedTokens } from '@instructure/ui-themes';
type StyleParams = {
    disabled: TextAreaProps['disabled'];
    readOnly: TextAreaProps['readOnly'];
    size: TextAreaProps['size'];
    success: boolean;
    invalid: boolean;
};
/**
 * ---
 * private: true
 * ---
 * Generates the style object from the theme and provided additional information
 * @param componentTheme The theme variable object.
 * @param params Additional parameters to customize the style.
 * @param sharedTokens Shared token object that stores common values for the theme.
 * @return The final style object, which will be used in the component
 */
declare const generateStyle: (componentTheme: ReturnType<NewComponentTypes["TextArea"]>, params: StyleParams, sharedTokens: SharedTokens) => TextAreaStyle;
export default generateStyle;
//# sourceMappingURL=styles.d.ts.map