import type { ThemeOrLegacyOverride } from '../EmotionTypes';
import type { DeterministicIdProviderValue } from '@instructure/ui-react-utils';
import type { NewThemeOverrideObject, Theme } from '@instructure/ui-themes';
type InstUIProviderProps = {
    children?: React.ReactNode;
    /**
     * A full theme or an override object. The override only works for legacy
     * (v11.6 or earlier) themes, for newer ones use the `themeOverride` prop.
     */
    theme?: ThemeOrLegacyOverride;
    /**
     * An override object for the new theming system. It will be deep merged into
     * the theme. One can override primitives, semantics and individual component's
     * themes, for example:
     * ```js
     * themeOverride={{
     *   semantics: {
     *     color: {
     *       stroke: {
     *         error: 'purple'
     *       }
     *     }
     *   },
     *   primitives: {
     *     color: {
     *       blue: {
     *         blue100: 'yellow'
     *       }
     *     }
     *   },
     *   components: {
     *     Alert: {
     *       background: 'brown',
     *       infoIconBackground: 'darkblue',
     *       borderWidth: '0.5rem'
     *     },
     *     Pill: {
     *       baseTextColor: 'purple',
     *       baseBorderColor: 'purple'
     *     }
     *   },
     *   sharedTokens: {
     *     focusOutline: {
     *       width: '0.55rem',
     *       infoColor: 'deeppink'
     *     }
     *   }
     * }}
     * ```
     */
    themeOverride?: NewThemeOverrideObject | ((theme: Theme) => NewThemeOverrideObject);
    /**
     * @deprecated the `instanceCounterMap` prop is deprecated. You don't need to supply the
     * `instanceCounterMap` to the component. It handles it internally.
     *
     * A [Map](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map) which keeps track of
     * specific InstUI components. (generally this is used for deterministic id generation for [SSR](/#server-side-rendering))
     */
    instanceCounterMap?: DeterministicIdProviderValue;
    /**
     * The text direction to use in the descendants. If not provided, it uses the following in this priority order:
     *   - The value given in a parent `TextDirectionContext`
     *   - The `dir` prop of `document.documentElement` or its `direction` CSS prop
     *   - The default `ltr`
     */
    dir?: 'ltr' | 'rtl';
};
/**
 * ---
 * category: components
 * ---
 * @module InstUISettingsProvider
 */
declare function InstUISettingsProvider({ children, theme, dir, instanceCounterMap, themeOverride }: InstUIProviderProps): import("@emotion/react/jsx-runtime").JSX.Element;
export default InstUISettingsProvider;
export { InstUISettingsProvider };
export type { InstUIProviderProps };
//# sourceMappingURL=index.d.ts.map