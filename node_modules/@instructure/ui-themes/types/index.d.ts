import type { ComponentTypes as NewComponentTypes } from './themes/newThemeTokens/componentTypes';
import type { SharedTokens, BaseTheme as NewBaseTheme, TokenBoxshadowValueInst, TokenTypographyValueInst } from './themes/newThemeTokens/commonTypes';
import type { CanvasHighContrastTheme } from './themes/canvasHighContrast';
import type { CanvasTheme, CanvasBrandVariables } from './themes/canvas';
import type { DarkTheme } from './themes/dark';
import type { LightTheme } from './themes/light';
import type { BaseTheme, Primitives, AdditionalPrimitives, DataVisualization, UI, DeepPartial } from '@instructure/shared-types';
import canvasHighContrast from './themes/canvasHighContrast';
import canvas from './themes/canvas';
import dark from './themes/dark';
import light from './themes/light';
import { primitives, additionalPrimitives } from './legacySharedThemeTokens/colors/primitives';
import dataVisualization from './legacySharedThemeTokens/colors/dataVisualization';
import { boxShadowObjectsToCSSString } from './utils/boxShadowObjectToString';
import type { LegacyCanvas as NewCanvas, LegacyCanvasHighContrast as NewCanvasHighContrast, Dark, Light } from './themes/newThemeTokens';
type ThemeMap = {
    canvas: CanvasTheme;
    'canvas-high-contrast': CanvasHighContrastTheme;
    [k: string]: BaseTheme;
};
type ThemeKeys = keyof ThemeMap;
type NewComponentsAsValue = {
    [K in keyof NewComponentTypes]: ReturnType<NewComponentTypes[K]>;
};
type NewThemeOverrideObject = {
    primitives?: Record<string, any>;
    semantics?: Record<string, any>;
    sharedTokens?: DeepPartial<SharedTokens>;
    components?: DeepPartial<NewComponentsAsValue>;
};
type Theme<NewThemeType extends NewBaseTheme = NewBaseTheme, K extends ThemeKeys = ThemeKeys> = BaseTheme & {
    newTheme: NewThemeType;
} & {
    key: K;
} & {
    themeOverride?: NewThemeOverrideObject;
} & Partial<CanvasBrandVariables>;
type ThemeSpecificStyle<ComponentTheme> = {
    [themeKey in ThemeKeys]?: Partial<ComponentTheme>;
};
export { dark, light, canvas, canvasHighContrast, primitives, additionalPrimitives, dataVisualization, boxShadowObjectsToCSSString };
export default canvas;
export type { ThemeMap, BaseTheme, Theme, ThemeKeys, ThemeSpecificStyle, CanvasTheme, CanvasBrandVariables, CanvasHighContrastTheme, DarkTheme, LightTheme, Primitives, AdditionalPrimitives, DataVisualization, UI, NewCanvas, NewCanvasHighContrast, Dark, Light, NewComponentTypes, NewBaseTheme, NewThemeOverrideObject, TokenBoxshadowValueInst, TokenTypographyValueInst, SharedTokens };
//# sourceMappingURL=index.d.ts.map