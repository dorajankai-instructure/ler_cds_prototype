import legacySharedThemeTokens from '../../legacySharedThemeTokens';
import { Colors } from '@instructure/shared-types';
import { type LegacyCanvasHighContrast as NewCanvasHighContrast } from '../newThemeTokens';
import { Theme } from '../../index';
export type CanvasHighContrastTheme = Theme<NewCanvasHighContrast, 'canvas-high-contrast'> & typeof legacySharedThemeTokens & {
    colors: Colors;
};
/**
 * Canvas high contrast theme without the `use` function and `variables` prop.
 * Not affected by global theme overrides (`.use()` function).
 *
 * Will be default in the next major version of InstUI
 */
declare const theme: CanvasHighContrastTheme;
export default theme;
//# sourceMappingURL=index.d.ts.map