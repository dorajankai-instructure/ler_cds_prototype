import type { NewComponentTypes, SharedTokens } from '@instructure/ui-themes';
import type { SpinnerProps, SpinnerStyle } from './props';
type StyleParams = {
    size: SpinnerProps['size'];
    variant: SpinnerProps['variant'];
    margin: SpinnerProps['margin'];
};
/**
 * ---
 * private: true
 * ---
 * Generates the style object from the theme and provided additional information
 * @param  {Object} componentTheme The theme variable object.
 * @param params Additional parameters to customize the style.
 * @param sharedTokens Shared token object that stores common values for the theme.
 * @return {Object} The final style object, which will be used in the component
 */
declare const generateStyle: (componentTheme: ReturnType<NewComponentTypes["Spinner"]>, params: StyleParams, sharedTokens: SharedTokens) => SpinnerStyle;
export default generateStyle;
//# sourceMappingURL=styles.d.ts.map